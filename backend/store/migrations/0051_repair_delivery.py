"""
M12 / BR-005E — the handover.

`RepairDelivery`, one per order, plus the `delivered` lifecycle code.

NO PAYMENT COLUMN, and that is a finding rather than an omission.
`PaymentTransaction.order` is a non-null FK to the e-commerce `Order`, with no
tenant column and no generic relation, so a `RepairOrder` cannot be paid through
it as the schema stands. A `paid` flag here would be a claim this software
cannot support. Service payment is its own phase.

NO SIGNATURE AND NO PHOTO. DEC-016 still has no storage provider, and a
`conformity` boolean with nothing behind it would assert consent nobody
captured.

The seeding of the new status for companies that already exist is the NEXT
migration, and the capability grant the one after. Same triptych as M8 through
M11.
"""
# Generated by Django 5.2.17 on 2026-09-02 19:26

import django.db.models.deletion
import django.utils.timezone
from django.conf import settings
from django.db import migrations, models


class Migration(migrations.Migration):

    dependencies = [
        ('store', '0050_quality_capability_for_untouched_presets'),
        migrations.swappable_dependency(settings.AUTH_USER_MODEL),
    ]

    operations = [
        migrations.AlterField(
            model_name='repairorder',
            name='status',
            field=models.CharField(choices=[('received', 'Recibido'), ('diagnosing', 'En diagnóstico'), ('waiting_approval', 'Esperando aprobación'), ('approved', 'Aprobado'), ('rejected', 'Rechazado'), ('in_repair', 'En reparación'), ('waiting_parts', 'Esperando repuestos'), ('repaired', 'Reparado'), ('quality_control', 'En control de calidad'), ('ready_for_pickup', 'Listo para recoger'), ('delivered', 'Entregado'), ('cancelled', 'Cancelado')], db_index=True, default='received', max_length=32),
        ),
        migrations.AlterField(
            model_name='repairstatushistory',
            name='from_status',
            field=models.CharField(blank=True, choices=[('received', 'Recibido'), ('diagnosing', 'En diagnóstico'), ('waiting_approval', 'Esperando aprobación'), ('approved', 'Aprobado'), ('rejected', 'Rechazado'), ('in_repair', 'En reparación'), ('waiting_parts', 'Esperando repuestos'), ('repaired', 'Reparado'), ('quality_control', 'En control de calidad'), ('ready_for_pickup', 'Listo para recoger'), ('delivered', 'Entregado'), ('cancelled', 'Cancelado')], max_length=32),
        ),
        migrations.AlterField(
            model_name='repairstatushistory',
            name='to_status',
            field=models.CharField(choices=[('received', 'Recibido'), ('diagnosing', 'En diagnóstico'), ('waiting_approval', 'Esperando aprobación'), ('approved', 'Aprobado'), ('rejected', 'Rechazado'), ('in_repair', 'En reparación'), ('waiting_parts', 'Esperando repuestos'), ('repaired', 'Reparado'), ('quality_control', 'En control de calidad'), ('ready_for_pickup', 'Listo para recoger'), ('delivered', 'Entregado'), ('cancelled', 'Cancelado')], max_length=32),
        ),
        migrations.AlterField(
            model_name='repairstatussetting',
            name='code',
            field=models.CharField(choices=[('received', 'Recibido'), ('diagnosing', 'En diagnóstico'), ('waiting_approval', 'Esperando aprobación'), ('approved', 'Aprobado'), ('rejected', 'Rechazado'), ('in_repair', 'En reparación'), ('waiting_parts', 'Esperando repuestos'), ('repaired', 'Reparado'), ('quality_control', 'En control de calidad'), ('ready_for_pickup', 'Listo para recoger'), ('delivered', 'Entregado'), ('cancelled', 'Cancelado')], db_index=True, max_length=32),
        ),
        migrations.CreateModel(
            name='RepairDelivery',
            fields=[
                ('id', models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name='ID')),
                ('recipient_name', models.CharField(max_length=160)),
                ('notes', models.TextField(blank=True, max_length=1000)),
                ('delivered_at', models.DateTimeField(default=django.utils.timezone.now)),
                ('created_at', models.DateTimeField(auto_now_add=True)),
                ('idempotency_key', models.CharField(blank=True, default='', max_length=64)),
                ('request_fingerprint', models.CharField(blank=True, default='', max_length=64)),
                ('company', models.ForeignKey(on_delete=django.db.models.deletion.PROTECT, related_name='repair_deliveries', to='store.company')),
                ('delivered_by', models.ForeignKey(blank=True, null=True, on_delete=django.db.models.deletion.SET_NULL, related_name='repair_deliveries_made', to=settings.AUTH_USER_MODEL)),
                ('repair_order', models.OneToOneField(on_delete=django.db.models.deletion.PROTECT, related_name='delivery', to='store.repairorder')),
            ],
            options={
                'ordering': ['-delivered_at', '-pk'],
                'indexes': [models.Index(fields=['company', 'delivered_at'], name='store_repai_company_ea8919_idx')],
                'constraints': [models.UniqueConstraint(condition=models.Q(('idempotency_key', ''), _negated=True), fields=('company', 'idempotency_key'), name='unique_delivery_idempotency_key_per_company')],
            },
        ),
    ]
