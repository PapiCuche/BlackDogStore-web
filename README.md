# BlackDogStore-web

Monorepo scaffold: `frontend/` (Next.js) and `backend/` (Django + DRF).

Development (with Docker):

```bash
docker-compose up --build
```

Frontend dev: `http://localhost:3000`
Django admin: `http://localhost:8000/admin`
